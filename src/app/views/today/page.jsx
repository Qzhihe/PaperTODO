import { Fragment } from "react";
import { redirect } from "next/navigation";

export default function Page(props) {
  // redirect("/");

  return (
    <Fragment>
      <div>Today.js</div>
    </Fragment>
  );
}
